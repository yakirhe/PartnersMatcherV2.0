<UserControl x:Class="PartnersMatcher.View.Controls.SideMenuC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PartnersMatcher.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Name="searchActivityBtn" Click="searchActivityBtn_Click">Search activity</Button>
        <Button Name="addActivityBtn" Grid.Row="1" Click="addActivityBtn_Click">Add activity</Button>
        <Button Name="logOutBtn" Grid.Row="2" Click="logOutBtn_Click">Log out</Button>
    </Grid>
</UserControl>
